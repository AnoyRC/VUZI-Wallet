import "hashes/sha256/256bitPadded" as sha256;

def main(private u32[8] a, u32[4][8] recoveryHashes ) -> bool {
    u32[8] h = sha256(a);

    assert(h == recoveryHashes[0] || h == recoveryHashes[1] || h == recoveryHashes[2] || h == recoveryHashes[3]);
    return true;
}