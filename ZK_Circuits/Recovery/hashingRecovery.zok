import "hashes/sha256/256bitPadded" as sha256;

def main(private u32[8] a, private u32[8] b, private u32[8] c, private u32[8] d) -> u32[4][8] {
    u32[8] h1 = sha256(a);
    u32[8] h2 = sha256(b);
    u32[8] h3 = sha256(c);
    u32[8] h4 = sha256(d);

    return [h1, h2, h3, h4];
}